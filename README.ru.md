<p align="center">
  <img src="docs/logo.svg?v=5" width="120" height="120" alt="Aura Logo" />
</p>

<h1 align="center">Aura — голосовой ввод для Windows</h1>

<p align="center">
  <b>Мгновенная офлайн и облачная голосовая печать прямо под курсором.</b><br />
  <a href="https://aura-beryl-five.vercel.app/">🌐 Официальный сайт и веб-демо</a>
</p>

<p align="center">
  <a href="https://github.com/malashkadev/aura/actions/workflows/ci.yml"><img src="https://github.com/malashkadev/aura/actions/workflows/ci.yml/badge.svg" alt="CI" /></a>
  <a href="https://github.com/malashkadev/aura/releases"><img src="https://img.shields.io/github/v/release/malashkadev/aura?color=10b981&label=релиз" alt="Релиз" /></a>
  <a href="LICENSE"><img src="https://img.shields.io/badge/License-AGPL_v3-blue.svg" alt="Лицензия: AGPL v3" /></a>
  <img src="https://img.shields.io/badge/platform-Windows%2010%2F11-blue" alt="Платформа" />
</p>

> 🇬🇧 [English documentation](README.md)

Зажмите горячую клавишу, надиктуйте текст и отпустите — Aura мгновенно расшифрует речь и напечатает готовый текст прямо под курсором в **любом** приложении Windows. Работает полностью автономно на локальных моделях (Whisper, NVIDIA Parakeet с CUDA-ускорением на GPU) или подключается к облачным сервисам (Gemini, Groq, OpenAI, Hugging Face, Custom API).

**100% бесплатно и с открытым исходным кодом (AGPL-3.0)** — без подписок, скрытых ограничений, встроенной рекламы и телеметрии.

<p align="center">
  <img src="docs/Aura.gif" width="49%" alt="Демонстрация работы Aura" />
  <img src="docs/Settings.gif" width="49%" alt="Настройки Aura" />
</p>

## Возможности

- **Диктовка по глобальной горячей клавише** — удерживайте для записи (`Alt+V`) или включите режим переключателя (короткое нажатие); отмена по `Esc`.
- **Два режима распознавания**:
  - **Локальный (100% офлайн и приватно)** — whisper.cpp или NVIDIA Parakeet TDT v3 (sherpa-onnx) на CPU или GPU NVIDIA CUDA. Звук не покидает ваш компьютер; скачивайте модели в один клик прямо из настроек.
  - **Облачный** — Google Gemini, Groq, OpenAI, Hugging Face или собственный OpenAI-совместимый сервер.
- **Аппаратное ускорение NVIDIA CUDA** — поддержка GPU для моделей Whisper и Parakeet с автоматической загрузкой runtime-библиотек в один клик и безопасным переключением на CPU при необходимости.
- **Мгновенный потоковый ввод** — стабильный пословный ввод текста в реальном времени без мерцания и дублирования.
- **Защита фокуса (Focus Guard) и контекстное редактирование** — защищает от ввода в чужие окна при случайной смене фокуса (с безопасным переносом текста в буфер обмена) и позволяет быстро редактировать выделенный фрагмент с визуальным бейджем-искоркой.
- **Выбор микрофона и защита от обрыва фраз** — переключение устройства ввода в настройках, 160-мс буфер удержания звука и Silero VAD, предотвращающие обрезание окончаний слов.
- **История транскрипций** — последние 50 записей со строкой живого поиска, фильтрацией по источникам (`Все` / `Локально` / `Облако`) и копированием в один клик.
- **Собственный словарь** — подсказка специфических имён, брендов и технических терминов для безошибочного написания.
- **11 вариантов языка** — автоопределение, привязка к текущей раскладке клавиатуры или фиксированный выбор (ru, en, de, es, fr, it, zh, pt, tr).
- **Компактный оверлей** — индикация уровня микрофона, таймер записи, звуковые темы (Дзен, Rhodes, Sci-Fi, Классика) и гибкая настройка отображения.
- **Системная интеграция и безопасность** — автозапуск с Windows, меню в системном трее, системное шифрование ключей (DPAPI) и встроенная выгрузка диагностического отчёта.

## Сравнение

Aura сочетает автономную скорость локальных нейросетей, поддержку GPU-ускорения и гибкость подключения облачных сервисов.

| Параметр | **Aura** | **Handy** | **Wispr Flow** |
|---|---|---|---|
| **Стоимость** | **Бесплатно (AGPL-3.0)** | Бесплатно (Open Source) | Платно (подписка) |
| **Платформы** | Windows *(macOS в разработке)* | Windows / macOS / Linux | Windows / macOS |
| **Локальные модели (Офлайн)** | **✅ Whisper & Parakeet (CUDA GPU / CPU)** | ✅ Whisper / Parakeet | ❌ Только облако |
| **Облачные провайдеры** | **✅ Gemini, Groq, OpenAI, HF, Custom** | ❌ | ✅ Собственное облако |
| **Потоковый ввод в реальном времени** | **✅** | ❌ | ✅ |
| **Защита фокуса (Focus Guard)** | **✅** | ❌ | ➖ |
| **Шифрование данных (DPAPI)** | **✅** | ❌ | ➖ |
| **Пользовательский словарь терминов** | **✅** | ❌ | ✅ |
| **История с поиском и фильтрацией** | **✅** | ❌ | ✅ |

> Официальные сборки Aura создаются для Windows 10 и 11 (macOS-порт компилируется в CI). Handy — зрелое кроссплатформенное решение для офлайн-диктовки. Wispr Flow предлагает качественный интерфейс, но требует платную подписку и отправляет звук в закрытое облако.

## Установка

Скачайте установщик из [Releases](https://github.com/malashkadev/aura/releases) и запустите. Ознакомиться с интерактивным макетом настроек и посмотреть демонстрацию работы можно на [Официальном сайте](https://aura-beryl-five.vercel.app/).

Для локального режима скачайте подходящую модель прямо из вкладки «Голос» в настройках программы (модель `Base` — отличный старт для быстрого CPU-распознавания). Для облачного режима укажите API-ключ выбранного сервиса (у Groq и Google Gemini доступны бесплатные лимиты).

> **При первом запуске «Система Windows защитила ваш компьютер»?** Установщик пока не подписан сертификатом Authenticode, поэтому Windows SmartScreen выводит стандартное предупреждение для новых программ. Нажмите **«Подробнее» → «Выполнить в любом случае»**. Исходный код полностью открыт для аудита и самостоятельной сборки.

## Использование

| Действие | По умолчанию |
|---|---|
| Начать запись | зажать `Alt + V` |
| Завершить и напечатать текст | отпустить клавишу |
| Режим переключателя (фиксация записи) | короткое нажатие `Alt + V` |
| Отменить запись | `Esc` |

Горячая клавиша, язык, движок и параметры оверлея настраиваются в окне настроек (иконка в трее → «Открыть настройки»).

## Сборка из исходников

Требования: [Rust](https://rustup.rs/) (stable), [Node.js](https://nodejs.org/) 18+, WebView2 (встроен в Windows 10/11).

```bash
git clone https://github.com/malashkadev/aura.git
cd aura
npm install
npm run dev     # режим разработки
npm run build   # установщик NSIS/MSI в src-tauri/target/release/bundle/
```

Бинарники whisper.cpp находятся в `src-tauri/binaries/`. Для обновления до свежего релиза выполните `python install_whisper.py`.

Запуск тестов:

```bash
cd src-tauri
cargo test
```

## Приватность и безопасность

- **Локальный режим** работает полностью автономно на вашем компьютере (CPU / GPU) и не передаёт аудиозаписи в сеть.
- **Облачный режим** отправляет аудио, полученный транскрипт, выделенный текст (только при включённом редактировании выделения) и словарь терминов напрямую выбранному провайдеру по TLS без сторонней телеметрии или аналитики.
- **Безопасное локальное хранение** — API-ключи и история транскрипций шифруются с помощью **Windows DPAPI** (`CryptProtectData`), а доступ к файлам изолирован на уровне ACL для текущего пользователя и SYSTEM.
- **Проверка обновлений** выполняется вручную или только при её явном включении в настройках.

## Недавно добавлено

- **Аппаратное ускорение NVIDIA CUDA** — высокоскоростная работа локальных моделей (Whisper и Parakeet) на GPU с установкой runtime-библиотек в один клик и автоматическим переключением на CPU.
- **Выбор микрофона и VU-метр** — переключение микрофона прямо в настройках с индикатором уровня сигнала и Silero VAD детекцией в реальном времени.
- **Защита от обрыва окончаний (Tail Hold)** — 160-мс буфер удержания звука после отпускания клавиши и расширенное окно Silero VAD, предотвращающие обрезание последних слогов.
- **Контекстное редактирование и безопасный перенос** — визуальный индикатор-искорка для редактирования выделения; при смене фокуса текст безопасно копируется в буфер обмена с уведомлением в оверлее.
- **Интеграция с Google Gemini 3.6 Flash** — переход на мультимодальную модель Gemini 3.6 Flash для мгновенного распознавания и голосового редактирования текста.
- **Кастомизация оверлея и Topmost Guard** — возможность скрыть таймер и статус, оставив компактную капсулу звуковых волн, и защита поверх всех окон (`HWND_TOPMOST`).
- **Мгновенное скрытие оверлея** — звуковой сигнал и скрытие оверлея срабатывают без задержки сразу при вставке текста.

## Планы развития

- **Поддержка macOS** — нативный порт (глобальные хоткеи через `CGEventTap`, захват CoreAudio) уже находится в кодовой базе и **компилируется в CI**. До релиза предстоит сборка macOS-бинарника whisper, оформление `.app`-бандла и тестирование на живых устройствах.

## Лицензия

[AGPL-3.0](LICENSE)
